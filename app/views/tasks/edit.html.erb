<div class="row">
	<div class="span6 offset3" >
		<% @page_title = "Edit Task: #{@task.subject}" -%>

		<div class="well">
			<%= form_for(:task, :url => {:action => 'update', :id => @task.id} , :html =>{:class=>"form-horizontal"}) do |f| %>
			<legend>
				Edit Task
			</legend>
			<%= render(:partial => 'form', :locals => {:f => f} ) %>

			<div class="form-buttons pull-right" >
				<%= link_to("Back to List", {:action => 'list'}, :class => 'btn btn-inverse') %>
				<%= submit_tag 'Update Task', :class =>"btn btn-primary" %>
			</div>
<br>
			<% end %>
		</div>
	</div>
</div>
