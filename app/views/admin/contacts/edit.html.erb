<div class="row">
	<div class="span6 offset3" >
		<% @page_title = "Edit Contact: #{@contact.name}" -%>

		<div class="well">
			<%= form_for(:contact, :url => {:action => 'update', :id => @contact.id} , :html =>{:class=>"form-horizontal"}) do |f| %>
			<legend>
				Edit Contact
			</legend>
			<%= render(:partial => 'form', :locals => {:f => f} ) %>

			<div class="form-buttons pull-right" >
				<%= link_to("Back to List", {:action => 'list'}, :class => 'btn btn-inverse') %>
				<%= submit_tag 'Update Contact', :class =>"btn btn-primary" %>
			</div>
<br>
			<% end %>
		</div>
	</div>
</div>
