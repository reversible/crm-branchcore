<div class="row">
	<div class="span6 offset3" >
		<% @page_title = "Edit Account: #{@account.name}" -%>

		<div class="well">
			<%= form_for(:account, :url => {:action => 'update', :id => @account.id} , :html =>{:class=>"form-horizontal"}) do |f| %>
			<legend>
				Edit account
			</legend>
			<%= render(:partial => 'form', :locals => {:f => f} ) %>

			<div class="form-buttons pull-right" >
				<%= link_to("Back to List", {:action => 'list'}, :class => 'btn btn-inverse') %>
				<%= submit_tag 'Update Account', :class =>"btn btn-primary" %>
			</div>
<br>
			<% end %>
		</div>
	</div>
</div>
