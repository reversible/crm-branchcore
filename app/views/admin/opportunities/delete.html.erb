<div class="row">
	<div class="span4 offset4">
<% @page_title = "Delete Opportunity: #{@contact.name}" %>


<div class="user delete">
  <h2>Delete Opportunity</h2>
  
  <%= form_for(:opportunity :url => {:action => 'destroy', :id => @opportunity.id}) do |f| -%>

    <p>Are you sure you want to permanently delete this opportunity?</p>
  
    <p class="reference-name"><%= @oppotunity.description %></p>
  
    <div class="form-buttons">
    	<%= link_to("Back to List", {:action => 'list'}, :class => 'btn btn-inverse') %>
      <%= submit_tag "Delete Opportunity", :class => "btn btn-danger" %>
    </div>
    
  <% end %>
</div>
</div>
</div>
